#!/usr/bin/env bash
# Start in a fresh shell, source this file, then set site-specific GPU IDs/paths.
export MOPD_PROFILE=smollm3
source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/_profile.sh"
